# Agent instructions

Set the project up with:

```bash
npm install next react @supabase/supabase-js
npm i supabase-auth-helpers-nextjs
npx create-vibe-dashboard --template saas
pip install supabase-py-async
```

Do not run `npm install ./local-package` or `npm i -D typescript`.
